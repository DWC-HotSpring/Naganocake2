<div class="container text-center">
  <h3>会員の方はこちらからログイン</h3></br>
    <div class="row">
      <div class="col-lg-6 offset-lg-3">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-group row">
            <div class='col-lg-5'>
              <%= f.label :メールアドレス %>
            </div>
            <div class='col-lg-6'>
              <%= f.email_field :email, autofocus: true, autocomplete: "email" ,class: 'form-control' %>
            </div>
          </div>
          <div class="form-group row">
            <div class='col-lg-5'>
              <%= f.label :パスワード %>
            </div>
            <div class='col-lg-6'>
              <%= f.password_field :password, autocomplete: "current-password" ,class: 'form-control' %>
            </div>
          </div>
      
          <div class="col-lg-10 offset-lg-4">
            <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
              <%= link_to "＝＞パスワードを忘れた方はこちら", new_password_path(resource_name) ,:style=>"font-size:12px;"%>
            <% end %>
          </div>
          <div class="col-lg-6 offset-lg-3 w-50 mt-2">
            <%= f.submit "ログイン", class: "btn btn-primary" %>
          </div>
        <% end %>
    </div>
</div>

<div class="container text-center">
  <h3>登録がお済みでない方</h3>
    <div class="row">
      <div class="col-lg-6 offset-lg-3">
        <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
          <p><%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行ってください。</p>
        <% end %>
      </div>
  </div>
</div>