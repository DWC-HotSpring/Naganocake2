<h1>ジャンル編集</h1>
<div class="container">
    <div class="row">
        <div class = "col-xs-5 col-xs-offset-1">
            <%= form_with model:[:admins,@genre], local: true do |f| %>
                <%= f.text_field :name %>
                    <span>有効</span><%= f.radio_button :is_active, :true %>
                    <span>無効</span><%= f.radio_button :is_active, :false %>
                <%= f.submit %>
            <% end %>
        </div>
    </div>
</div>